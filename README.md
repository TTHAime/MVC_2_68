# ระบบจัดสรรที่พักพิงในสถานการณ์ฉุกเฉิน

## Emergency Shelter Assignment System

โปรเจกต์นี้พัฒนาโดยใช้ **MVC Design Pattern** ด้วย Java Swing และ SQLite Database

---

## 📋 Features

1. **ลงทะเบียนประชาชน**
   - ลงทะเบียนประชาชนพร้อมข้อมูลส่วนตัว
   - แสดงรายการประชาชนทั้งหมด
   - กรองตามประเภทประชาชน (ทั่วไป, กลุ่มเสี่ยง, VIP)

2. **จัดสรรที่พักพิง**
   - แสดงรายละเอียดศูนย์พักพิง (ความจุ, จำนวนคนปัจจุบัน, ระดับความเสี่ยง)
   - จัดสรรอัตโนมัติตาม Business Rules
   - จัดสรรด้วยตัวเองแบบ Manual

3. **รายงานผล**
   - แสดงประชาชนที่ได้รับการจัดสรรแล้ว
   - แสดงประชาชนที่ยังไม่ได้รับการจัดสรร (ตกค้าง)
   - สรุปภาพรวมระบบ

---

## 🏗️ โครงสร้างโปรเจกต์

```
MVC_2_68/
├── src/
│   ├── model/
│   │   ├── Citizen.java          # Model สำหรับประชาชน
│   │   ├── Shelter.java          # Model สำหรับศูนย์พักพิง
│   │   └── Assignment.java       # Model สำหรับการจัดสรร
│   ├── database/
│   │   └── DatabaseManager.java  # จัดการ SQLite Database
│   ├── controller/
│   │   ├── CitizenController.java
│   │   ├── ShelterController.java
│   │   └── AssignmentController.java
│   ├── view/
│   │   ├── CitizenRegistrationView.java    # View 1: ลงทะเบียนประชาชน
│   │   ├── ShelterAssignmentView.java      # View 2: จัดสรรที่พักพิง
│   │   ├── ReportView.java                 # View 3: รายงานผล
│   │   └── MainFrame.java                  # Main Window
│   └── Main.java                 # Entry Point
├── lib/
│   └── sqlite-jdbc-3.42.0.0.jar  # SQLite JDBC Driver (ต้อง download)
├── compile.bat                    # Script สำหรับ compile
└── run.bat                        # Script สำหรับ run
```

---

## ✅ Requirements ที่ตรงตาม Specification

### 1. MVC Design Pattern ✓

- **Model**: Citizen, Shelter, Assignment
- **View**: 3 Views แยกกันชัดเจน
- **Controller**: CitizenController, ShelterController, AssignmentController

### 2. Database (SQLite) ✓

- **Shelters**: รหัสศูนย์พักพิง, ความจุสูงสุด, ระดับความเสี่ยง, จำนวนคนปัจจุบัน
- **Citizens**: รหัสประชาชน, อายุ, สภาวะสุขภาพ, วันที่ลงทะเบียน, ประเภท
- **Assignments**: รหัสประชาชน, รหัสศูนย์พักพิง, วันที่เข้าพักพิง, หมายเหตุ

### 3. Views (3 หน้าจอ) ✓

- หน้าลงทะเบียนประชาชน (แสดงทั้งหมด + แยกตามประเภท)
- หน้าจัดสรรพักพิง (แสดงรายละเอียดพักพิง + จำนวนคนปัจจุบัน)
- หน้ารายงานผล (แสดงรายละเอียดประชาชนที่ได้พักและไม่ได้พัก)

### 4. Business Rules ✓

- ศูนย์พักพิงที่เต็มแล้วไม่สามารถรับเพิ่มได้
- เด็กและผู้สูงอายุได้รับการจัดสรรก่อน (Priority System)
- ผู้มีความเสี่ยงด้านสุขภาพต้องถูกจัดไปยังศูนย์ความเสี่ยงต่ำ
- ประชาชนหนึ่งคนลงทะเบียนได้เพียงครั้งเดียว

### 5. ข้อมูลตัวอย่าง ✓

- ศูนย์พักพิง: **8 แห่ง** (> 5)
- ประชาชน: **35 คน** (≥ 30)
- มีประชาชนที่ไม่ได้รับการจัดสรร (ทั้ง 35 คนเริ่มต้นยังไม่ได้รับการจัดสรร)

---

## 🚀 Quick Start (สำหรับคนที่ Clone จาก Git)

### วิธีที่ 1: ใช้ Script อัตโนมัติ (แนะนำ)

```powershell
# ดาวน์โหลด JDBC Driver และ Compile ในคำสั่งเดียว
.\setup.bat
```

จากนั้นรันโปรแกรม:

```batch
.\run.bat
```

### วิธีที่ 2: ทำทีละขั้นตอน

**1. ดาวน์โหลด SQLite JDBC Driver:**

```powershell
.\download-jdbc.ps1
```

**2. Compile โปรเจกต์:**

```batch
.\compile.bat
```

**3. Run โปรแกรม:**

```batch
.\run.bat
```

---

## 🔧 วิธีการติดตั้งและรัน (รายละเอียด)

### ขั้นตอนที่ 1: ดาวน์โหลด SQLite JDBC Driver

1. ไปที่ [https://github.com/xerial/sqlite-jdbc/releases](https://github.com/xerial/sqlite-jdbc/releases)
2. ดาวน์โหลดไฟล์ `sqlite-jdbc-3.42.0.0.jar` (หรือเวอร์ชันล่าสุด)
3. วางไฟล์ใน folder `lib/`

**หรือใช้คำสั่ง (ใน PowerShell):**

```powershell
New-Item -ItemType Directory -Force -Path lib
Invoke-WebRequest -Uri "https://repo1.maven.org/maven2/org/xerial/sqlite-jdbc/3.42.0.0/sqlite-jdbc-3.42.0.0.jar" -OutFile "lib/sqlite-jdbc-3.42.0.0.jar"
```

### ขั้นตอนที่ 2: Compile โปรแกรม

```batch
compile.bat
```

### ขั้นตอนที่ 3: Run โปรแกรม

```batch
run.bat
```

---

## 📝 วิธีใช้งาน

1. **ลงทะเบียนประชาชน**
   - เปิด Tab "ลงทะเบียนประชาชน"
   - กรอกข้อมูล: ชื่อ, อายุ, สภาวะสุขภาพ, ประเภท
   - กดปุ่ม "ลงทะเบียน"
   - ระบบจะมีข้อมูลตัวอย่าง 35 คนอยู่แล้ว

2. **จัดสรรที่พักพิง**
   - เปิด Tab "จัดสรรที่พักพิง"
   - เลือกประชาชนจากตาราง "ประชาชนที่รอการจัดสรร"
   - กด "จัดสรรอัตโนมัติ" หรือเลือกศูนย์พักพิงแล้วกด "จัดสรรด้วยตัวเอง"

3. **ดูรายงานผล**
   - เปิด Tab "รายงานผล"
   - ดูสรุปภาพรวม
   - ดูรายการประชาชนที่ได้รับและไม่ได้รับการจัดสรร

---

## 💡 Code Quality

### การแบ่ง Class ที่ดี

- แยก Model, View, Controller อย่างชัดเจน
- แต่ละ Class มีหน้าที่เฉพาะ (Single Responsibility)
- DatabaseManager จัดการ Database แยกต่างหาก
- Business Logic อยู่ใน Model และ Controller

### Clean Code

- ตั้งชื่อตัวแปรและ method ชัดเจน
- แยก method ย่อยเพื่อความอ่านง่าย
- มี Comments อธิบาย
- Error Handling ที่เหมาะสม
- Validation ข้อมูล

---

## 📦 Technologies

- **Java 8+** (Swing GUI)
- **SQLite 3** (Database)
- **JDBC** (Database Connectivity)
- **MVC Pattern** (Architecture)

---

## 👨‍💻 Development Notes

โปรเจกต์นี้พัฒนาตามหลักการ:

- **Separation of Concerns**: แยก Business Logic, Data Access, UI
- **Encapsulation**: ซ่อนรายละเอียดการทำงานภายใน
- **Maintainability**: โค้ดอ่านง่าย แก้ไขง่าย
- **Extensibility**: ขยายฟังก์ชันได้ง่าย

---

## 📄 License

โปรเจกต์นี้สร้างขึ้นเพื่อการศึกษา
