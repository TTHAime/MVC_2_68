# 📘 Setup Guide - คู่มือติดตั้งสำหรับผู้ Clone โปรเจกต์

## ✅ สิ่งที่ต้องมีก่อนเริ่มต้น

- **Java Development Kit (JDK) 8 ขึ้นไป**
  - ตรวจสอบว่ามี Java ติดตั้งแล้วหรือยัง:
    ```batch
    java -version
    javac -version
    ```
  - ถ้ายังไม่มี ดาวน์โหลดได้ที่: https://adoptium.net/

- **PowerShell** (สำหรับ Windows - มีติดตั้งมาแล้ว)

---

## 🚀 วิธีติดตั้งแบบเร็ว (3 步)

### 1. Clone โปรเจกต์

```bash
git clone <repository-url>
cd MVC_2_68
```

### 2. รัน Setup Script

```batch
setup.bat
```

Script นี้จะ:
- ✓ ดาวน์โหลด SQLite JDBC Driver อัตโนมัติ
- ✓ Compile โปรเจกต์ทั้งหมด
- ✓ พร้อมใช้งานทันที

### 3. รันโปรแกรม

```batch
run.bat
```

---

## 🔧 วิธีติดตั้งแบบละเอียด (ทีละขั้นตอน)

### ขั้นตอนที่ 1: Clone และเข้าโฟลเดอร์

```bash
git clone <repository-url>
cd MVC_2_68
```

### ขั้นตอนที่ 2: ดาวน์โหลด SQLite JDBC Driver

**วิธีที่ 1: ใช้ PowerShell Script (แนะนำ)**
```powershell
powershell -ExecutionPolicy Bypass -File download-jdbc.ps1
```

**วิธีที่ 2: ดาวน์โหลดด้วยตัวเอง**
1. ไปที่ https://github.com/xerial/sqlite-jdbc/releases
2. ดาวน์โหลด `sqlite-jdbc-3.42.0.0.jar`
3. วางไฟล์ในโฟลเดอร์ `lib/` (สร้างโฟลเดอร์ถ้ายังไม่มี)

### ขั้นตอนที่ 3: Compile โปรเจกต์

```batch
compile.bat
```

จะเห็นข้อความ "Compilation successful!" ถ้าสำเร็จ

### ขั้นตอนที่ 4: รันโปรแกรม

```batch
run.bat
```

---

## 📁 โครงสร้างไฟล์หลังติดตั้ง

```
MVC_2_68/
├── src/               # Source code (มีใน Git)
├── lib/
│   └── sqlite-jdbc-3.42.0.0.jar  # ดาวน์โหลดมาใหม่ (ไม่มีใน Git)
├── bin/               # Compiled classes (สร้างใหม่)
├── shelter_system.db  # Database (สร้างอัตโนมัติเมื่อรัน)
├── setup.bat          # Setup script
├── compile.bat        # Compile script
├── run.bat            # Run script
└── download-jdbc.ps1  # Download script
```

---

## ❓ FAQ - คำถามที่พบบ่อย

### Q: ทำไม lib/ ว่างเปล่า?
**A:** ไฟล์ .jar ไม่ถูกเก็บใน Git เพื่อลดขนาด repository  
ต้องดาวน์โหลดเองหลัง clone โดยใช้ `download-jdbc.ps1`

### Q: เกิดข้อผิดพลาด "javac is not recognized"
**A:** ต้องติดตั้ง JDK และตั้งค่า PATH environment variable  
ดู: https://www.java.com/en/download/help/path.html

### Q: เกิดข้อผิดพลาด "UnsupportedClassVersionError"
**A:** Java version ที่ใช้รันต่ำกว่าที่ใช้ compile  
แก้ไข: compile.bat ให้ระบุ `-source 8 -target 8`

### Q: โปรแกรมรันแล้วไม่มีข้อมูล
**A:** ปกติโปรแกรมจะสร้างข้อมูลตัวอย่าง 35 คนอัตโนมัติในครั้งแรก  
ถ้าไม่มี ให้ลบไฟล์ `shelter_system.db` แล้วรันใหม่

### Q: ต้องการลบข้อมูลทั้งหมดเริ่มใหม่
**A:** ลบไฟล์ `shelter_system.db` แล้วรันโปรแกรมใหม่

---

## 🐛 Troubleshooting

### ปัญหา: PowerShell บล็อกการรัน script

**อาการ:** `cannot be loaded because running scripts is disabled`

**วิธีแก้:**
```powershell
# รันด้วยคำสั่งนี้แทน
powershell -ExecutionPolicy Bypass -File download-jdbc.ps1
```

### ปัญหา: Compile ไม่ผ่าน - encoding errors

**อาการ:** ข้อความภาษาไทยแสดงผิดพลาด

**วิธีแก้:** compile.bat ได้ระบุ `-encoding UTF-8` ไว้แล้ว  
ถ้ายังมีปัญหา ให้บันทึกไฟล์เป็น UTF-8 encoding

### ปัญหา: Database locked

**อาการ:** `database is locked`

**วิธีแก้:** 
1. ปิดโปรแกรมทุก instance
2. ลบไฟล์ `shelter_system.db-journal` ถ้ามี
3. รันโปรแกรมใหม่

---

## 📞 ต้องการความช่วยเหลือ?

อ่านรายละเอียดเพิ่มเติมใน [README.md](README.md)

---

## 📝 หมายเหตุสำหรับผู้พัฒนา

### การ Commit ไฟล์ใหม่

ไฟล์เหล่านี้ **ไม่ควร** commit ขึ้น Git:
- `bin/` - compiled classes
- `lib/*.jar` - JDBC driver
- `*.db` - database files
- `*.class` - compiled files

ดูรายการเต็มใน [.gitignore](.gitignore)

### การแก้ไขโค้ด

หลังแก้ไขไฟล์ในโฟลเดอร์ `src/`:
```batch
compile.bat
run.bat
```
